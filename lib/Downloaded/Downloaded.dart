import 'package:flutter/material.dart';

class Downloaded extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Ini Downloaded"),
    );
  }
}